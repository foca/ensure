#!/usr/bin/env bash

ensure rbenv
ensure package ruby-build

eval "$(rbenv init -)";

version="$1";
shift

if $(rbenv versions | grep -q "\b${version}\b"); then
  exit 0;
fi

rbenv install "$version" $@
